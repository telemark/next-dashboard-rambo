{
  "name": "next-dashboard-rambo",
  "description": "Dashboard for statuses",
  "version": "3.0.5",
  "license": "MIT",
  "private": true,
  "author": {
    "name": "Geir Gåsodden",
    "email": "geir.gasodden@pythonia.no",
    "url": "https://github.com/zrrrzzt"
  },
  "engines": {
    "node": ">=10.8.0"
  },
  "scripts": {
    "test": "standard && npm audit && ava",
    "test-offline": "standard && ava",
    "coverage": "nyc ava",
    "coveralls": "nyc ava && nyc report --reporter=lcov && cat coverage/lcov.info | coveralls",
    "standard-fix": "standard --fix",
    "dev": "next",
    "build": "next build",
    "export": "next export",
    "start": "next start",
    "deploy": "npm test && npm run build && npm run export && now --static out --name dashboard-rambo && now alias",
    "refresh": "rm -rf node_modules && rm package-lock.json && npm install",
    "cleanup": "rm -rf .next && rm -rf out"
  },
  "keywords": [
    "dashboard",
    "next",
    "status",
    "rambo",
    "Telemark fylkeskommune"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/telemark/next-dashboard-rambo.git"
  },
  "bugs": {
    "url": "https://github.com/telemark/next-dashboard-rambo/issues"
  },
  "homepage": "https://github.com/telemark/next-dashboard-rambo#readme",
  "devDependencies": {
    "ava": "1.2.0",
    "coveralls": "3.0.2",
    "nyc": "13.2.0",
    "standard": "12.0.1"
  },
  "dependencies": {
    "axios": "0.18.0",
    "next": "7.0.2",
    "react": "16.8.0",
    "react-dom": "16.8.0"
  }
}
